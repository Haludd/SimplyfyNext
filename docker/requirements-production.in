# Direct production requirements.  Keep this file aligned with [project].dependencies.
# The Linux builder resolves transitive dependencies and platform wheels; the macOS
# pylock.toml is intentionally not used by the production image.
anthropic>=0.104,<1
boto3>=1.35,<2
fastapi>=0.115,<1
httpx>=0.27,<1
langgraph==1.2.11
pydantic>=2.9,<3
pydantic-settings>=2.6,<3
uvicorn>=0.30,<1
